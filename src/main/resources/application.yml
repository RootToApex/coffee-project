spring:
  datasource:
    url: jdbc:mysql://localhost:3307/coffee
    username: coffee_user
    password: caffeine
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show-sql: true
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379